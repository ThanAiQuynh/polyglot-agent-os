# Rust
target/
build_temp/
Cargo.lock

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local
.env.*.local

# Beads (Note: .beads/ is git-backed and SHOULD be committed, 
# but temporary/cache files if any could be ignored. 
# According to Beads docs, the JSONL is the DB, so we keep it.)
# .beads/sqlite.db # SQLite cache for Beads (if generated locally)

# Agent context
.antigravity/
